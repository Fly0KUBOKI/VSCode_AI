# AI使用量モニター 要件定義書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | AI使用量モニター 要件定義書 |
| バージョン | 1.0 |
| 作成日 | 2026年8月21日 |
| 対象フェーズ | MVP（初期版） |
| 対象利用者 | 開発者本人（単一ユーザー） |

## 2. 背景

VS Code上でChatGPT CodexおよびClaude Codeを利用する際、現在の利用率、残りの利用余力、制限期間ごとの状態が分かりにくい。そのため、意図せず利用上限へ到達し、作業を継続できなくなる可能性がある。

本システムは、複数のAIサービスの利用状況をVS Code内に集約し、現在の利用率を継続的に可視化することで、この不透明さを軽減する。

## 3. 目的

- CodexとClaude Codeの現在の利用率を、それぞれ独立して確認できるようにする。
- 複数の利用制限枠が存在する場合、制限枠ごとの利用率を確認できるようにする。
- 利用率が設定したしきい値へ達したことをVS Code内で通知する。
- 高頻度の自動更新と手動更新を提供する。
- 取得値の出所と信頼度を明示し、実測値と推定値を混同させない。
- VS CodeおよびPCへの負荷を低く保つ。

## 4. 対象範囲

### 4.1 MVPの対象

| 項目 | 対象 |
|---|---|
| OS | Windows 10、Windows 11 |
| エディター | デスクトップ版Visual Studio Code |
| Codex契約 | ChatGPT Plus |
| Codex実行方法 | VS Code公式拡張 |
| Claude契約 | Claude Pro |
| Claude Code実行方法 | VS Code公式拡張 |
| 表示対象 | 現在の利用率、残り率、制限枠、最終更新日時、取得状態 |
| 通知先 | VS Code通知センター |
| 履歴 | ローカル保存 |
| 配布 | ローカルVSIX |

### 4.2 MVPの対象外

- スマートフォンへの通知
- VS Code終了中の監視
- PCの電源断またはスリープ中の監視
- macOSおよびLinux対応
- VS Code Marketplaceへの一般公開
- 複数ユーザーまたはチームの利用量集計
- API従量課金額の管理
- プロンプト、回答、会話、ソースコードの保存
- CodexおよびClaude Code以外のAIサービス

スマートフォン通知は将来拡張として扱う。将来実装時は、Discord Webhook、ntfyなどの外部通知サービスを候補とする。

## 5. 用語定義

| 用語 | 定義 |
|---|---|
| 利用率 | 対象となる制限枠のうち、既に消費した割合。0～100%で表す |
| 残り率 | `100 - 利用率`。取得元が残量形式の場合は取得値を優先する |
| 制限枠 | 短期間枠、週単位枠など、サービスが個別に管理する利用上限の単位 |
| 実測値 | 公式APIまたは公式に提供された機械可読情報から直接取得した値 |
| 準実測値 | 公式クライアント、公式拡張、公式CLI、公式画面の表示から取得した値 |
| 推定値 | ローカル履歴などから計算した値 |
| 不明 | 十分な情報を取得できず、信頼できる利用率を表示できない状態 |
| リセット | 制限期間が切り替わり、利用可能量が回復すること |

## 6. システム概要

本システムはTypeScriptで実装するVS Code拡張機能とする。独立した常駐アプリケーションは設けず、VS Code Extension Host上で動作する。

```text
VS Code起動
    ↓
拡張機能を自動起動
    ↓
前回値を即時表示
    ↓
Codex / Claude Codeの利用情報を非同期取得
    ↓
共通データ形式へ変換
    ├─ ステータスバー更新
    ├─ 詳細画面更新
    ├─ 履歴保存
    └─ しきい値判定・VS Code通知
```

## 7. 機能要件

### FR-01 自動起動

1. VS Code起動時に拡張機能を自動的に有効化する。
2. 起動直後は、保存済みの最終取得値を暫定表示する。
3. 起動後、CodexおよびClaude Codeの最新利用率を非同期で取得する。
4. 取得処理によってVS Codeの起動や操作をブロックしない。

### FR-02 Codex利用率の取得

1. ChatGPT Plusに含まれるCodexの利用率を取得する。
2. 複数の制限枠が存在する場合は、取得可能なすべての枠を取得する。
3. 取得方法は、次の優先順位とする。
   1. 公式APIまたは公式の機械可読インターフェース
   2. Codex公式拡張または公式CLIが出力する利用情報
   3. Codexがローカルに保存した利用関連データ
   4. 公式Usage画面に表示される情報
   5. ローカル履歴に基づく推定
4. 取得方法と信頼度をスナップショットへ記録する。
5. 認証情報そのものは収集または複製しない。

### FR-03 Claude Code利用率の取得

1. Claude Proに含まれるClaude Codeの利用率を取得する。
2. ClaudeとClaude Codeで共有される制限枠は、共有枠であることを明示する。
3. 複数の制限枠が存在する場合は、取得可能なすべての枠を取得する。
4. 取得方法は、次の優先順位とする。
   1. 公式APIまたは公式の機械可読インターフェース
   2. Claude Code公式拡張または公式CLIが出力する利用情報
   3. Claude Codeがローカルに保存した利用関連データ
   4. 公式利用状況画面に表示される情報
   5. ローカル履歴に基づく推定
5. 取得方法と信頼度をスナップショットへ記録する。
6. 認証情報そのものは収集または複製しない。

### FR-04 自動更新

1. 標準の自動更新間隔は1分とする。
2. 利用者は更新間隔を設定で変更できる。
3. 最小更新間隔は、取得元の安全性に応じてProviderごとに制限できる。
4. 公式画面の読み取りを使用する場合は、アクセス負荷を避けるため原則5分以上とする。
5. 前回の取得が完了していない場合、新しい取得処理を開始しない。
6. 同時に複数の更新要求が発生した場合、1回の処理へまとめる。
7. 連続失敗時は指数バックオフを適用し、成功後に標準間隔へ戻す。
8. VS Codeが非アクティブの場合も、起動している間は設定された間隔で更新する。

### FR-05 手動更新

1. 次の場所から手動更新できるようにする。
   - ステータスバー項目
   - 詳細画面の更新ボタン
   - コマンドパレットの `AI Usage: Refresh`
2. 更新中は進行状態を表示する。
3. 手動更新でも多重実行を防止する。
4. Provider単位の更新と全Providerの一括更新に対応する。

### FR-06 ステータスバー表示

1. CodexとClaude Codeの利用率を別々に表示する。
2. 合算利用率は表示しない。
3. 各サービスに複数の制限枠がある場合、最も残り率が少ない枠を代表値として表示する。
4. 表示例を次に示す。

```text
Codex 64%  Claude 38%
```

5. 取得中、取得失敗、データ期限切れ、推定値を識別できる表示とする。
6. ステータスバーをクリックすると詳細画面を開く。
7. 状態分類は次のとおりとする。

| 利用率 | 状態 |
|---:|---|
| 0～69% | 正常 |
| 70～84% | 注意 |
| 85～94% | 警告 |
| 95～99% | 制限間近 |
| 100% | 上限到達 |

### FR-07 詳細画面

1. VS Code内のWebviewとして表示する。
2. CodexとClaude Codeを別セクションで表示する。
3. 取得可能なすべての制限枠を表示する。
4. 各制限枠について、次の情報を表示する。
   - サービス名
   - 制限枠の名称
   - 現在の利用率
   - 残り率
   - リセット予定日時（取得可能な場合）
   - 最終取得日時
   - 取得方法
   - 実測、準実測、推定、不明の区分
   - 取得状態およびエラー
5. 利用率の時系列グラフを表示する。
6. Provider単位および全体の手動更新ボタンを表示する。
7. データが古い場合は、古い値であることを明示する。

### FR-08 VS Code通知

1. 次の利用率へ初めて到達したときに通知する。
   - 70%
   - 85%
   - 95%
   - 100%
2. 通知しきい値は設定で変更可能とする。
3. 同一制限期間中は、同じしきい値の通知を繰り返さない。
4. 利用率が一度に複数のしきい値を超えた場合、到達した最大しきい値について1回だけ通知する。
5. リセットを検出した場合に通知する。
6. 一定時間以上取得できない場合、データ取得エラーを通知する。
7. 単発の通信失敗では通知せず、連続失敗時のみ通知する。

### FR-09 リセットの検出

1. 公式情報からリセットを特定できる場合は、その情報を優先する。
2. 公式情報から特定できない場合は、次の条件を組み合わせて判定する。
   - 利用率が高い状態から大幅に低下した。
   - リセット予定時刻を経過した。
   - 複数回の取得で低下後の状態を確認した。
3. 1回の異常値だけでリセットと確定しない。
4. リセット後は、その制限枠の通知済みしきい値を初期化する。
5. リセット予定日時が不明な場合は、日時を推測して確定値として表示しない。

### FR-10 履歴保存

1. 利用状況をローカルへ保存する。
2. 保存項目は次のとおりとする。
   - Provider ID
   - サービス名
   - 制限枠IDおよび名称
   - 取得日時
   - 利用率
   - 残り率
   - リセット予定日時
   - 取得方法
   - 信頼度区分
   - 取得成否
   - エラー種別
3. プロンプト、回答、会話内容、ソースコードは保存しない。
4. 標準保存期間は30日とする。
5. 保存期間は設定で変更可能とする。
6. 保存期限を超えた履歴は自動削除する。

### FR-11 設定

次の項目をVS Codeの設定画面から変更できるようにする。

| 設定 | 初期値 |
|---|---:|
| 自動更新 | 有効 |
| 自動更新間隔 | 60秒 |
| Codex Provider | 有効 |
| Claude Code Provider | 有効 |
| 通知しきい値 | 70、85、95、100% |
| リセット通知 | 有効 |
| 取得エラー通知 | 有効 |
| 履歴保存期間 | 30日 |
| 推定値表示 | 有効 |
| デバッグログ | 無効 |

### FR-12 エラー処理

1. 取得に失敗しても直前の正常値を保持する。
2. 直前値には取得日時を併記し、最新値と誤認させない。
3. 次のエラーを可能な範囲で区別する。
   - 公式拡張が未導入または無効
   - 未ログインまたは認証切れ
   - ネットワーク障害
   - 取得元のアクセス制限
   - データ形式の変更
   - データが存在しない
   - 権限不足
   - 不明なエラー
4. エラーの詳細に認証情報や個人情報を含めない。
5. 取得不能時に架空の利用率を生成しない。

### FR-13 コマンド

最低限、次のコマンドを提供する。

- `AI Usage: Open Dashboard`
- `AI Usage: Refresh`
- `AI Usage: Refresh Codex`
- `AI Usage: Refresh Claude Code`
- `AI Usage: Open Settings`
- `AI Usage: Clear History`
- `AI Usage: Show Logs`

履歴削除は確認操作を伴うものとする。

## 8. データ要件

### 8.1 共通データモデル

```ts
type Confidence = 'measured' | 'semi-measured' | 'estimated' | 'unknown';

interface UsageWindow {
  id: string;
  label: string;
  usedPercent?: number;
  remainingPercent?: number;
  resetsAt?: string;
}

interface UsageSnapshot {
  providerId: 'codex' | 'claude-code';
  capturedAt: string;
  source: string;
  confidence: Confidence;
  windows: UsageWindow[];
  status: 'ok' | 'stale' | 'unavailable' | 'error';
  errorCode?: string;
}
```

### 8.2 整合性

- 利用率と残り率は0～100%の範囲に正規化する。
- 取得元が残り率のみを返す場合は、利用率を算出したことを内部メタデータへ記録する。
- 異なる制限枠の値を合算しない。
- CodexとClaude Codeの値を合算しない。
- タイムスタンプは内部ではISO 8601形式で保存し、画面ではWindowsのローカルタイムで表示する。

## 9. 非機能要件

### NFR-01 パフォーマンス

- アイドル時に継続的なCPU処理を行わない。
- 定期更新以外はイベント駆動とする。
- ポーリング処理は非同期で実行する。
- Webviewは詳細画面を開いた場合のみ生成する。
- 履歴を画面表示する際は、必要な期間だけ読み込む。
- 通常利用時にVS Codeの操作感へ認識可能な遅延を生じさせない。

### NFR-02 セキュリティ

- 既存のCodexまたはClaude Code認証情報を複製しない。
- 独自に秘密情報を保存する必要がある場合はVS Code `SecretStorage`を使用する。
- Cookie、OAuthトークン、セッショントークンを平文保存しない。
- 秘密情報をログ、通知、Webviewへ出力しない。
- 外部コマンドへ動的文字列を渡す場合は、コマンドインジェクションを防止する。
- WebviewにはContent Security Policyを設定する。

### NFR-03 プライバシー

- 利用履歴は利用者のPC内だけに保存する。
- テレメトリーを実装しない。
- 外部サーバーへ利用履歴を送信しない。
- 保存対象外の会話内容やコードを読み取らない。

### NFR-04 保守性

- CodexとClaude Codeの取得処理をProviderとして分離する。
- 取得元の仕様変更時に対象Providerだけを交換できる構成とする。
- UIはProvider固有のデータ形式へ直接依存しない。
- パーサーには取得元別のテストデータを用意する。ただし、秘密情報をテストデータへ含めない。

```ts
interface UsageProvider {
  readonly id: string;
  isAvailable(): Promise<boolean>;
  fetchUsage(): Promise<UsageSnapshot>;
}
```

### NFR-05 信頼性

- すべての表示値へ信頼度区分を関連付ける。
- 取得元の形式変更を検知した場合、前回値を最新値として扱わない。
- 推定値には「推定」と表示する。
- 不明な値を0%または100%として表示しない。
- 一方のProviderが失敗しても、もう一方のProviderの更新を継続する。

### NFR-06 ログ

- 通常ログには、処理開始・終了、取得方法、成否、所要時間、エラー種別を記録する。
- デバッグログは利用者が明示的に有効化した場合だけ詳細化する。
- ログへ認証情報、Cookie、プロンプト、回答、コードを出力しない。

## 10. UI要件

### 10.1 ステータスバー

- CodexとClaude Codeを別々に識別できること。
- 表示領域を過度に占有しないこと。
- 最も制約の厳しい利用枠を代表値とすること。
- ツールチップで全制限枠、最終更新日時、信頼度を確認できること。

### 10.2 詳細画面

- 各サービスのカードを並べて表示する。
- 各制限枠をプログレスバーで表示する。
- 正常、注意、警告、上限到達を色だけに依存せず、文字またはアイコンでも示す。
- キーボード操作で主要機能を利用できること。
- VS CodeのLight/Dark/High Contrastテーマへ追従すること。

### 10.3 表示例

```text
Codex
  短期間枠       64%使用  残り36%  準実測
  週単位枠       42%使用  残り58%  準実測

Claude Code
  共有利用枠     38%使用  残り62%  推定

最終更新: 2026/08/21 10:15:00
```

## 11. 技術構成

| 項目 | 採用技術 |
|---|---|
| 言語 | TypeScript |
| 実行環境 | VS Code Extension Host / Node.js |
| UI | VS Code StatusBarItem、Webview |
| 永続化 | VS Code `globalState` または拡張機能用ストレージ |
| 秘密情報 | VS Code `SecretStorage` |
| ビルド | TypeScript、esbuild |
| パッケージ | VSIX |
| テスト | 単体テスト、Providerパーサーテスト、VS Code拡張統合テスト |

## 12. 技術検証要件

ChatGPT PlusおよびClaude Proの購読枠には、第三者拡張が利用できる安定した公開Usage APIが存在しない、または利用できない可能性がある。そのため、本実装の前に次の技術検証を実施する。

1. Codex公式拡張が表示する利用率を安全に取得できるか。
2. Claude Code公式拡張が表示する利用率を安全に取得できるか。
3. 公式CLIに、追加のAIリクエストを発生させず利用率だけを返す手段があるか。
4. 公式クライアントのローカルデータに利用率または制限枠情報が含まれるか。
5. 認証情報を直接読み出さずに取得できるか。
6. 取得結果が各サービスの公式画面と一致するか。
7. 1分間隔の取得がアクセス制限、規約、PC負荷の観点で許容されるか。
8. 取得元のデータ形式が変更された場合に安全に失敗できるか。

技術検証で正確な利用率を取得できないProviderについては、次の順でMVPの挙動を決定する。

1. 準実測値を表示する。
2. 推定値を明記して表示する。
3. 信頼できる推定もできない場合は「取得不可」と表示し、公式Usage画面への導線を提供する。

## 13. 制約・前提

- 本システムは各公式拡張の内部API利用を前提にしない。利用可能性は技術検証で判断する。
- 公式拡張または公式画面の仕様変更により、取得機能が一時的に利用不能になる場合がある。
- サービスが正確な残量を公開しない場合、正確な利用率を保証できない。
- Claude Proの利用枠はClaude本体とClaude Codeで共有される場合があり、本システム外での利用も利用率へ反映される。
- VS Code終了中およびPCスリープ中は更新・通知を行わない。
- 利用規約またはセキュリティ上問題のある取得方法は、技術的に可能でも採用しない。

## 14. 受け入れ基準

### AC-01 起動と表示

- VS Code起動後、拡張機能が自動起動する。
- 保存済みの最終値が存在する場合、最新取得の完了前に表示される。
- CodexとClaude Codeが別々に表示される。

### AC-02 更新

- 標準設定で約1分ごとに更新処理が実行される。
- 手動更新を実行できる。
- 更新操作を連続しても取得処理が多重実行されない。
- 一方のProviderの失敗が他方の更新を停止させない。

### AC-03 複数制限枠

- 複数制限枠を取得できた場合、詳細画面にすべて表示される。
- ステータスバーには最も残り率が少ない枠が表示される。
- 異なる枠やサービスの利用率が合算されない。

### AC-04 通知

- 70%、85%、95%、100%へ初めて到達した際に通知される。
- 同一制限期間中、同一しきい値の通知が重複しない。
- リセット後、通知済み状態が初期化される。
- リセット検出時に通知される。

### AC-05 履歴

- 利用率と取得日時がローカルへ保存される。
- VS Code再起動後も保存済み履歴を表示できる。
- 保存期間を超えた履歴が削除される。
- プロンプト、回答、会話、コードが保存されない。

### AC-06 エラーと信頼度

- 取得失敗時に前回値とその取得日時が表示される。
- 実測、準実測、推定、不明を画面上で識別できる。
- 信頼できるデータがない場合に架空の利用率が表示されない。
- ログへ秘密情報が出力されない。

### AC-07 負荷

- 待機中にビジーループを行わない。
- 詳細画面を閉じている間、Webview由来の継続処理が残らない。
- 通常操作を妨げる同期I/Oまたは長時間処理をExtension Host上で実行しない。

## 15. 開発優先順位

### フェーズ0：技術検証

- Codex利用率取得経路の確認
- Claude Code利用率取得経路の確認
- 取得精度、更新可能頻度、認証情報への非依存性の評価

### フェーズ1：MVP

- 拡張機能の自動起動
- Codex / Claude Code Provider
- ステータスバー表示
- 1分間隔の自動更新
- 手動更新
- ローカル履歴
- しきい値通知
- リセット検出
- エラー表示

### フェーズ2：改善

- 詳細グラフの改善
- Providerの仕様変更検出
- 診断画面とログ出力の改善
- 設定のインポート・エクスポート

### 将来フェーズ

- スマートフォン通知
- VS Code終了中のクラウド監視
- macOS / Linux対応
- Marketplace公開

## 16. 完了条件

MVPは、次の条件をすべて満たしたとき完了とする。

1. フェーズ0の技術検証結果が記録されている。
2. CodexとClaude Codeについて、取得可能な最良の利用率または取得不可状態を正直に表示できる。
3. ステータスバー、詳細画面、自動更新、手動更新、履歴、通知が動作する。
4. すべての受け入れ基準を満たす。
5. VSIXとしてインストールおよびアンインストールできる。
6. セットアップ手順と既知の制約がREADMEに記載されている。
